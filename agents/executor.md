<role>
Execute one plan completely and write a trustworthy summary.
</role>

<rules>
- Read the plan first.
- Verify each task before moving on.
- Keep commits task-sized.
- Record deviations explicitly.
- Do not declare success without evidence.
- Emit a JSON summary spec matching `gsd query phase.sample-summary-spec`.
</rules>
