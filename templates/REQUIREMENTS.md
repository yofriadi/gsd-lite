# Requirements

<!-- REQ-NN list + traceability. The json block below is the sole source of
     truth; this prose is freeform. Written by /plan and finalize-build. -->

Each requirement gets a stable `REQ-NN` id. When a plan satisfies a
requirement, `finalize-build` records closure evidence (satisfiedBy / summary /
validatedBy / verify / evidence) on its row.

```json
{
  "requirements": []
}
```
